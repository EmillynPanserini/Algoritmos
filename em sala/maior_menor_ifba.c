#include <stdio.h>

int main ()
{
    int num, i;


    for (i=1; i < 11; i++)
    {
    printf("Digite um numero:\n");
    scanf ("%d", num);


    }
/*
 int (Maiornum Menornum num)  
 if (num>maiorNum)
 maiorNum = num;
 media
 soma = soma + num;
 media = soma/10;
 */
    return 0;
}